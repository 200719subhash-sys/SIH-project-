LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=

# Source admin endpoints (local/development gating only, NOT production authentication)
SOURCE_ADMIN_ENABLED=false

# OCR provider (optional; "none" or "tesseract")
OCR_PROVIDER=none
